<section class="profile">
  <h1>In User Page</h1>
  <h2>{{user.username}}</h2>
  {{#if user.songs}}
    <div class="songs">
      {{#each user.songs}}
         {{> 'song'}}
      {{/each}}
    </div>
  {{else}}
    <p>No Songs yet</p>
  {{/if}}
</section>